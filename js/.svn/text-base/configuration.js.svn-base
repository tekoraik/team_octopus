/**
 * Current configuration of the system.
 *      {Number} nFloors is the number of floors in the building.
 *      {Number} nElevator is the number of elevators in the building.
 *      {Array} aElevatorNames is a list of all the names of the elevators.
 */
var Namespace = Namespace || {};
Namespace.context = Namespace.context || {};
Namespace.context.configuration = {
	nFloors: 2,
    nElevators: 1,
    aElevatorNames: ["A"],
};